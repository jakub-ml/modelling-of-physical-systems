name: lab02
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello
      - run: pip install nbconvert
      - uses: actions/checkout@v4
      - run: sudo apt-get install pandoc texlive-xetex
      - run: python -m nbconvert --to pdf --execute lab_2.ipynb
      - uses: action/upload artifact@v4
      - with:
          path: lab_2.pdf