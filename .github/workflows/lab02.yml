name: lab02
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo Hello
      - run: pip install nbconvert
      - run: python -m nbconvert --to pdf lab_2.ipynb
      - run: sudo apt-get install pandoc texlive-xetex